Luis activo en PC1

Objetivo actual:
Trabajar `close-control/selected-stream-routing` sobre `close-control` para que el stream seleccionado y el workflow superior ya muestren continuidad operativa hacia finance, treasury, compliance o document-control.

Archivo permitido:
- `frontend/arcont-suite/app/close-control/page.tsx`

Entregable esperado:
1. Revisar:
- `frontend/arcont-suite/app/close-control/page.tsx`

- reforzar la experiencia del `Selected stream` y del workflow superior
- hacer mas claro el ruteo operativo
- mejorar continuidad con finance, treasury, compliance y document-control
- no rehacer toda la pantalla

2. Prioridad puntual:
- que cierre/finanzas sepan a donde brincar primero desde el stream
- que el stream mande al modulo correcto segun su estado real
- mantenerlo listo para pruebas

3. Ejecutar:
- `export NVM_DIR="$HOME/.nvm" && . "$NVM_DIR/nvm.sh" && nvm use 24`
- `npx tsc --noEmit && npm run lint`

4. Mantener todo en `main`.
5. Reportar archivos tocados.

No hacer:
- no tocar `frontend/arcont-suite/app/post-sale/page.tsx`
- no tocar backend/api
- no tocar auth/login
- no tocar docs/coordination/antonio-live-task.txt
