Antonio activo en PC2

Objetivo:
Trabajar `post-sale/selected-case-routing` sobre `post-sale` para que el caso seleccionado ya explique por que importa, cual es su impacto y que modulo debe tomar el siguiente paso operativo.

Primero ejecutar:
export NVM_DIR="$HOME/.nvm" && . "$NVM_DIR/nvm.sh" && nvm use 24

Archivo permitido:
- `frontend/arcont-suite/app/post-sale/page.tsx`

Entregable exacto:
1. Revisar:
- `frontend/arcont-suite/app/post-sale/page.tsx`

- reforzar la tarjeta `Selected signal`
- aterrizar mejor el siguiente paso humano o modulo relacionado
- mantener la experiencia existente
- no rehacer toda la pagina

Prioridad puntual en este ciclo:
- que entrega/postventa entiendan que pasa despues del caso
- que la pantalla conecte mejor con compliance, quality, document-control o projects
- subir utilidad real de prueba

3. Ejecutar:
- `npm run build:web`

4. Trabajar siempre en `main`.
5. Reportar archivos tocados.

No hacer:
- no tocar `frontend/arcont-suite/app/close-control/page.tsx`
- no tocar backend/api
- no tocar auth/login
- no tocar docs/coordination/luis-live-task.txt
- no rehacer pantallas enteras sin necesidad
